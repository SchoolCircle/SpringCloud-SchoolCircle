server:
  port: 8001
spring:
  application:
    name: Gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true #与注册中心连接
          lower-case-service-id: true #服务名称全小写
    nacos:
      discovery:
        server-addr: localhost:8848

